import type { FooterConfig } from "../types/config";

export const footerConfig: FooterConfig = {
  enable: true, // 是否启用Footer HTML注入功能
  visitorStats: true, // 页脚访问量统计（不蒜子）
};

// 直接编辑 config/FooterConfig.html 文件来添加备案号等自定义内容
